package oop.src.com.dinesh.introduction;

public class Constructor {
    public static void main(String[] args) {
        /*
        Student1() is a special function inside the class
        But we did not create any function inside the class
        where is this Student1()  being called - this is called default
        Constructor is a special function that runs when you create object,and it allocates
        some varibales
         */

        /*
         The question here is // why we want to make our own constructors and bind the args to objects ?
         - Constructors allow you to set the initial state of the object as soon as it is created.
        */
    }
}
